import { Template, CompiledTemplate } from "@twilio/frame-ui";
import { Strings } from "./localization/Strings";
declare const typedTemplates: Strings<CompiledTemplate>;
export { typedTemplates as templates, Template, CompiledTemplate };
